# The Ascent — tutorial video: recording recipe + timed VO script

Companion to [ascent-taxonomy.md](ascent-taxonomy.md). The screen recording is
driven by the Ascent's built-in demo mode, so every take has identical timing
and the voice-over can be recorded (or regenerated) independently and laid on
top.

**Sync contract:** the timecodes below are generated by `DEMO_TIMELINE` in
`apps/mobile/src/app/(app)/ascent.tsx`. If you change one, change the other.

## Recording the screen track

1. Dev build in the iOS Simulator (iPhone 16 Pro, light mode), signed into the
   account with the Howe/Field tree — real data, no placeholder trees.
2. Navigate anywhere in the app, then start capture and immediately deep-link
   into demo mode:

   ```sh
   xcrun simctl io booted recordVideo --codec hevc ascent-tutorial.mov &
   xcrun simctl openurl booted "mobile://ascent?demo=1"
   ```

3. The screen loads at the base, settles for 1.5 s, then climbs on the
   timeline below. When it reaches the summit and the final hold ends
   (~1:18), stop the recording (`kill %1`).
4. Demo mode is `__DEV__`-only; it cannot trigger in TestFlight or release
   builds.

Target delivery: portrait 1080×2340, HEVC, burned-in captions, under ~25 MB.

## Timeline + narration

Timecodes are from the moment the demo link is opened (t = 0). Pace is
~2.3 words/second; each block is written to fit its hold with a beat of air.

| Time | Screen | Voice-over |
|---|---|---|
| 0:00–0:11 | **Base.** Your amber circle, name, birth year; the Pulse card below. | "This is the Ascent — your whole family tree as a climb. It starts with you, here at the base. The card below is the Pulse: how much of your tree is found, how much is proven, and where your effort matters most right now." |
| 0:11–0:14 | *Scrolling up to generations 1–2.* | *(pause)* |
| 0:14–0:24 | **Parents & grandparents.** Segmented bars, "2 of 2 found." | "Every generation is a row, and every segment is one ancestor who should exist — two parents, four grandparents, doubling every step. A colored segment means your tree names someone. A dark one is a gap: an ancestor history hasn't given up yet." |
| 0:24–0:27 | *Scrolling to the middle generations.* | *(pause)* |
| 0:27–0:36 | **Mid generations.** Era labels visible; brown segments. | "Brown means found but not yet proven. When a link is backed by a real source, it turns green — verified. And every row is stamped with its era, so you always know what years you're standing in." |
| 0:36–0:39 | *Scrolling to generation 6.* | *(pause)* |
| 0:39–0:51 | **The frontier.** Wall chip, beacon card with why-statement. | "Higher up, the records thin. A flag marks a wall — a line that stops because no one has named the next ancestor. And this is a beacon: the one wall where breaking through opens the most new ground. Every beacon comes with a research brief — a starting plan you can take to the archive, the library, or the other open tab." |
| 0:51–0:55 | *Scrolling to the summit.* | *(pause)* |
| 0:55–1:05 | **Summit.** "Here the records thin…" and the golden-thread line. | "This is the summit — eight generations up. One day a golden thread appears here: an unbroken, verified chain from you to someone born centuries ago." |
| 1:05–1:17 | **Summit, holding.** | "The Ascent reads your tree — the work still happens where your tree lives, on Ancestry or FamilySearch. Find something, add it there, export a fresh GEDCOM, and bring it back. Then watch the climb change. That's the loop — and this is your scoreboard." |

Runtime: ~1:17 plus end card.

## Production notes

- **Captions:** burn in the VO text; a large share of viewers watch muted.
- **End card (2–3 s):** the Witness wordmark and "Open the Ascent from the
  Explore tab."
- **VO source:** ElevenLabs is fine for iteration; record a human read for
  the shipped version. Keep takes at the table's pacing — the screen will not
  wait.
- **V1 honesty:** nothing is verified yet in V1, so green segments and golden
  threads won't appear on screen. The narration above says "when… it turns
  green" and "one day a golden thread appears" deliberately — future tense.
  Do not show mock green segments in the recording.
- **Delivery:** upload to a public Supabase Storage bucket and stream with
  `expo-video`; do not bundle the file into the app binary.
