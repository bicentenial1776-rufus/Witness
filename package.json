{
  "name": "witness",
  "version": "0.1.0",
  "private": true,
  "description": "Witness — Family History Intelligence. GEDCOM import, AI-powered analysis, temporal queries, geographic discovery.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "parse:sample": "tsx scripts/parse-sample.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.109.0"
  }
}
